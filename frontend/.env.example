# Muss identisch mit ADMIN_API_KEY in backend/.env sein.
# Achtung: Diese Datei wird nur beim Bauen des Images gelesen, nicht beim
# normalen Start - nach dem Ändern ist ein "docker compose up --build" nötig, "up" allein reicht nicht.
VITE_ADMIN_API_KEY=<Your ADMIN API Key>